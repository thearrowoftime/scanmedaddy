# Sample FortiGate 120G / FortiOS config — intentionally insecure (audit demo)
#config-version=FG120G-7.0.12-FW-build0523-230904:opmode=0:vdom=0:user=admin
#serial-number=FG120G1234567890
config system global
    set hostname "fg-120g-01"
    set admintimeout 60
end
config system admin
    edit "admin"
        set accprofile "super_admin"
        set password ENC redacted
    next
end
config system interface
    edit "port1"
        set vdom "root"
        set ip 192.168.10.1 255.255.255.0
        set allowaccess ping https http ssh telnet snmp
        set type physical
    next
end
config system snmp community
    edit 1
        set name "public"
        config hosts
            edit 1
                set ip 0.0.0.0 0.0.0.0
            next
        end
    next
end
config firewall policy
    edit 1
        set name "allow-all-any-any"
        set srcintf "any"
        set dstintf "any"
        set srcaddr "all"
        set dstaddr "all"
        set action accept
        set schedule "always"
        set service "ALL"
        set nat enable
    next
end
# Missing: config system ntp, config log syslogd
